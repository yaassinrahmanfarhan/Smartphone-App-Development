void main(){
  var distance, speed;
  distance = 25;
  speed = 40;
  var time= (distance) / (speed);
  time *= 60;
  print("Time taken to reach ${distance} km at the speed of ${speed} km/h is ${time} minutes");
}